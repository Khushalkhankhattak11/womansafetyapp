import 'package:flutter/material.dart';

class ChildRating extends StatelessWidget {
  const ChildRating({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Rating"),
      ),
    );
  }
}
