import 'package:flutter/material.dart';

class ChildChat extends StatelessWidget {
  const ChildChat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('chat'),
      ),
    );
  }
}
